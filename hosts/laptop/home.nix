{ ... }:
{
  imports = [
    # i need to setup profiles to import here 

  ];

  config = {
    home = {
      username = "rain";
      homeDirectory = "/home/rain";

      stateVersion = "25.11";
    };
  };

}
